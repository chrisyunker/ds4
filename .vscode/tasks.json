{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-ds4-debug",
      "type": "shell",
      "command": "make -j $(sysctl -n hw.logicalcpu)",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": []
    }
  ]
}
